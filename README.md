# angularbuild
hello world!!!!!
git init A

git init B
git remote add origin *address*
git pull origin main

cd B/
git remote add origin *address*
git pull origin main

cd ../A
nano README.md
git status
git add .
git commit -m "changes done from repo A"

